export enum OrderStatus {
  'not verified' = 0,
  'verified' = 1,
  pending = 2,
  'on delivery' = 3,
  done = 4,
  rejected = 5,
}

export enum WinnerStatus {
  Pending = 0,
  Contacted = 1,
  Delivered = 2,
  'Closed number' = 3,
  'Wrong Number' = 4,
}
